// SDK overlay structs — game memory layouts, never constructed by us
noConstructor:src/sdk/entity.h

// Schema field accessors reinterpret_cast uint8_t* → T* intentionally:
// the pointer arithmetic lands on real game memory of that type.
invalidPointerCast:src/sdk/entity.h

// useStlAlgorithm — raw loop is clearer here with early-exit + side effect
useStlAlgorithm:src/core/schema.cpp
useStlAlgorithm:src/features/movement/edgebug/edgebug.cpp
